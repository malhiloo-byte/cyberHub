# Security Policy

## نطاق الاستخدام

CyberOS منصة تعليمية وهندسية شخصية للأمن السيبراني. يجب استخدامها فقط على الأنظمة والأهداف التي يملك المستخدم حق اختبارها أو يملك تفويضًا صريحًا ومحددًا لاختبارها. وجود أداة أو أمر في المشروع لا يمنح إذنًا قانونيًا أو تشغيليًا لاستخدامه ضد طرف ثالث.

تعتمد النواة سياسة **Fail-Closed**: التفويض المفقود أو المنتهي أو غير المطابق للـScope والـTarget يؤدي إلى الرفض، وقواعد الاستبعاد الصريحة تتقدم على قواعد الإدراج.

## الإبلاغ عن ثغرة

لا تنشر تفاصيل ثغرة أمنية غير مُصلحة في issue عام. أرسل تقريرًا خاصًا إلى مالك المستودع عبر قناة خاصة مرتبطة بمستودع GitHub بعد ربطه، أو استخدم GitHub Private Vulnerability Reporting إن كان متاحًا. لا ترفق أسرارًا حقيقية أو بيانات مستخدمين؛ استخدم PoC محدودًا وقابلًا لإعادة الإنتاج في بيئة تملكها.

يجب أن يتضمن التقرير وصفًا واضحًا، المكوّن أو المسار المتأثر، خطوات إعادة الإنتاج الآمنة، التأثير المتوقع، وإصدار أو commit متأثر. قد يطلب فريق المشروع تفاصيل إضافية قبل تأكيد المشكلة.

## التوقعات

لا نطلب أو نقبل اختبارات تدميرية، persistence، exfiltration، denial-of-service، أو تشغيلًا على أهداف غير مصرح بها. لا تُجرِ scan أو exploit على بنية المشروع أو أي بنية خارجية دون موافقة مكتوبة ونطاق وزمن محددين.

## الأسرار والبيانات

لا تضع مفاتيح API أو كلمات مرور أو tokens أو قواعد بيانات محلية في Git. استخدم ملفات إعداد محلية مستثناة من `.gitignore`، وقم بتدوير أي سر تم commit له فورًا حتى لو حُذف لاحقًا من working tree.

## النطاق الحالي

Module 0 يركز على Domain وScope authorization وTask execution المحلي وSQLite وCLI. أدوات reconnaissance والاتصالات الشبكية ليست جزءًا من هذا النطاق، وستحتاج كل Module لاحق إلى مراجعة معمارية وأمنية مستقلة.
